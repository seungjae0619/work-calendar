# Backend 환경 변수 예시

# 관리자 비밀번호 (강력한 비밀번호로 설정)
ADMIN_PASSWORD=your_secure_password_here

# 데이터베이스 설정 (Docker Compose 로컬 개발용)
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=shift_db
DB_PORT=5433
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/shift_db

# 프론트엔드 URL (배포 시 Vercel URL로 설정)
FRONTEND_URL=http://localhost:5173

# Railway 환경 (자동 설정, 변경 금지)
# RAILWAY_ENVIRONMENT_NAME=production